
use std::collections::HashMap;

/*
Given a vector of ints return the mean, median and mode
*/
fn ex1(Vec<u32>& vector) -> HashMap {
    let mut results = HashMap::new();

    results.insert(String::from("mean"), -1);
    results.insert(String::from("median"), -1);
    results.insert(String::from("mode"), -1);


}

fn get_mean(Vec<u32>& vector) -> u32 {
    0
}
